# `htpasswd` command examples

## Create new htpasswd file

Advice: Dont mess with, just use `touch && chmod`, then add users.


## Add or update new user to an existing .htpasswd file:

`htpasswd /etc/apache2/.htpasswd newuser`

